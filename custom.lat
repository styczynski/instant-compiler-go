/* parity of positive integers by recursion */

void printBool(boolean x) {
  if (x) {
    printString("true");
  } else {
    printString("false");
  }
}

int main () {
  boolean a = true;
  boolean b = false;
  printBool(a || b) ;
  return 0 ;
}